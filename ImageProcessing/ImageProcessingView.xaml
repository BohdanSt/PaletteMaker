<UserControl x:Class="PaletteMaker.ImageProcessing.ImageProcessingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PaletteMaker.ImageProcessing"
             xmlns:emgu="clr-namespace:Emgu.CV.UI;assembly=Emgu.CV.UI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <WindowsFormsHost Grid.Column="0" Margin="20">
            <emgu:ImageBox x:Name="imageControl" SizeMode="Zoom" FunctionalMode="Everything"/>
        </WindowsFormsHost>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            

            <Button x:Name="buttonOpenImage" Grid.Row="3" Content="Open image"  FontSize="12" FontWeight="DemiBold"
                Margin="10" Width="150" Height="40" Click="buttonOpenImage_Click"/>
            <Button x:Name="buttonCancelChanges" Grid.Row="4" Content="Cancel changes" FontSize="12" FontWeight="DemiBold"
                Margin="10" Width="150" Height="40" Click="buttonCancelChanges_Click"/>
            <Button x:Name="buttonSaveImage" Grid.Row="5" Content="Save image" FontSize="12" FontWeight="DemiBold"
                Margin="10" Width="150" Height="40" Click="buttonSaveImage_Click"/>
        </Grid>
    </Grid>
</UserControl>
